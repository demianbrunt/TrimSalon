# Firebase Functions local configuration (example)
# Copy to `functions/.env` for local development. Do NOT commit `functions/.env`.
#
# NOTE: Google OAuth credentials are configured as Firebase Functions secrets
# (GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET). Do not put those keys in `.env` if
# you deploy from the same machine, or deploys can fail due to Cloud Run env var
# collisions.

# Email (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=noreply@trimsalon.nl
